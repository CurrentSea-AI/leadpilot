// Application configuration

export const defaultOffer = {
  price: 1500,
  timelineDays: "7–10",
  includes: [
    "Full redesign",
    "Mobile optimization",
    "Booking/contact flow cleanup",
  ],
};

export const config = {
  defaultOffer,
};

export default config;

